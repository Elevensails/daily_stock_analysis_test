# P1.5 增量 PRD — 报告自洽性「配对定位」（self_consistency 跨段矛盾精剥）

> 文档类型：**增量 PRD**（接在已完成的 U3 防幻觉闭环之上）
> 仓库：`Elevensails/daily_stock_analysis_test`（本地 `dsa-test/`）
> 上层 SOP：产品经理增量 PRD → 架构师增量设计 → 工程师实现 → QA 验证
> 关联文档：`后续任务速览_2026-08-01.md` 第三节「下一站 P1.5」

---

## 1. 项目信息

| 项 | 值 |
|----|----|
| **Language** | 中文（与需求一致） |
| **Programming Language** | Python 3.11+，仅依赖标准库（沿用 `src/core/validator.py`／`degrade.py` 约束，**非前端项目，不引入 Vite/React/MUI**） |
| **Project Name** | `p1_5_self_consistency_locator` |
| **增量基座** | U3 防幻觉闭环（`validator.py` 的 `locate_violations` + `degrade.py` 的 `assemble_degraded` + `repair.py` 的 `_try_degrade`/`repair_report` 已就绪） |
| **改动量预估** | 约 50–100 行（小改动，可走快速模式 / BugFix） |

### 原始需求复述
当前 U3 校验能识别「跨段矛盾」（如某段称「涨停」、另一段出现「-0.12%」），但 `locate_violations()` 对 `self_consistency` 违规采用「逐段复跑同一规则」的局部定位策略。由于矛盾双方分处不同段落，**没有任何单段能复现整篇矛盾**，于是回退为一条 `granularity="document"`（`paragraph_index=None`）的 segment。而 `degrade.py` 护栏 2 规定「存在 document 级违规 → 不可定位剥离面 → 回退 reject」，最终用户看到**空白报告**。

**目标**：让 validator 在 `self_consistency` 失败时做**跨段关键词配对搜索**：把含矛盾数字的 **evidence 段**定位为可剥 `paragraph` 级段（`paragraph_index`=evidence），并把含涨停/跌停标记的 **claim 段**作为 `related_paragraph_index` 上下文（不剥）；`degrade` 据此**只剥离 evidence 段**而非整篇，链路经 `repair → emit_degraded` 贯通，消除空白报告体验问题。

---

## 2. 产品定义

### Product Goals（3 个正交目标）
1. **G1 精准定位**：`self_consistency` 跨段矛盾必须产出 `paragraph` 级 **evidence 段**（待剥，含矛盾数字）并带 `related_paragraph_index` 指向 claim 段（含涨停/跌停标记，仅上下文），不再回退 document 级。
2. **G2 局部降级**：`degrade` 仅剥离被定位到的矛盾段，保留报告其余内容，复检通过即 `emit_degraded`。
3. **G3 链路贯通**：从 `validate → locate_violations → repair → degrade → emit_degraded` 端到端可用，构造探针应断言 `final_action == "emit_degraded"` 而非 `"reject"`。

### User Stories
- **US1**（防幻觉 owner）：作为校验负责人，我希望跨段「涨停 + 负收益」矛盾能被 pinpoint 到 evidence 待剥段（含矛盾数字）并标出 claim 关联段，以便只删 evidence 矛盾段、保住报告主体与涨停叙事。
- **US2**（终端用户）：作为查看日报的用户，我希望即使某两份结论互相打架，我仍能看到**其余完整、合规**的分析，而不是整篇空白。
- **US3**（QA）：作为测试者，我希望用一条固定矛盾探针断言「定位到段 + 降级只剥段不剥文档」，以便回归时守住该行为。
- **US4**（维护者）：作为后续开发者，我希望 `locate_violations` 对 `self_consistency` 的定位逻辑与 red_line / impossible_move 等其他 check 互不干扰。

---

## 3. 技术规范

### 3.1 增量改动点（落到具体函数）

#### A. `src/core/validator.py`（核心改动；与架构师 Bob《系统设计 P1.5》Part A §5.1 / 共享知识 #1 对齐）
- **`ViolationSegment` 加法字段（跨文件契约）**：新增 `related_paragraph_index: int | None`、`pairing: str | None`；`to_dict()` 在 `location` 内同步输出，向后兼容（只加不减）。
  - `paragraph_index` = **待剥离段（evidence，含矛盾数字）**，degrade 按此剥离；
  - `related_paragraph_index` = **矛盾来源段（claim，含涨停/跌停标记），仅上下文，degrade 不剥离**；
  - `pairing` ∈ `limit_up_vs_negative_pct | limit_down_vs_positive_pct`，供日志/前端分类。
- **新增内部 helper** `_nearest_claim()` + `_locate_self_consistency(paragraphs) -> list[ViolationSegment]`：
  - 独立扫描「claim 段」（含 涨停/跌停 标记）与「evidence 段」（含反向涨跌幅数值），将**每条 evidence 段与最近的 claim 段配对**；
  - 每条矛盾产出 **1 条 `paragraph` 级 segment**：`paragraph_index`=evidence 段（待剥）、`related_paragraph_index`=claim 段（上下文）、`pairing`=方向；
  - **关键**：claim 段**不**作为可剥 segment，仅作 `related` 上下文 —— 若把 claim 也定位为可剥段，degrade 会两端同剥，移除比飙升并重新触发空白报告（正好抵消 P1.5 根因）。
- **改 `locate_violations()` 的 `self_consistency` 分支**（现为 `elif check.name == "self_consistency": seg = _locate_by_recheck(...)`）：
  - 先保留现有 `_locate_by_recheck`（处理**单段内**自相矛盾，行为不变）；
  - 若单段复跑未命中（跨段矛盾），**改调 `_locate_self_consistency`** 产出配对段；
  - **按裁决「document 级不做」**：`self_consistency` 分支**不再回退 document 级 segment**（其余 check 的 document 级回退逻辑保持原样，不受影响）。

#### B. `src/core/degrade.py`（叠加式改动，剥离逻辑零改）
- **剥离仍以 `paragraph_index`（evidence 段）为准**，逻辑/护栏零改动；只剥 evidence 段，claim（related）段保留。
- `_seg_to_dict` 透传 `related_paragraph_index` / `pairing`（dataclass 走 `to_dict()` 已含；dict 路径补默认值），确保 `removed_segments` / jsonl `degraded_segments` 携带配对上下文。
- 护栏（max_removed_ratio=0.5 / document 级回退）保持不动；**因此探针样本须含足够段落使剥离比 <50%**（见 §4）。

#### C. `src/core/repair.py`（叠加式改动，链路贯通）
- `repair_report` 轮耗尽 / LLM 异常时调用 `_try_degrade` → `assemble_degraded` → `emit_degraded`，链路贯通。
- **小叠加改动**：`_segments_block` 渲染 `related_paragraph_index`，提示模型「关联段为矛盾来源，请一并核对」，使定向修订可定位矛盾两端（测试 `test_prompt_contains_related_segment_hint` 覆盖）。
- 剥离目标仍只认 `paragraph_index`（evidence），`related` 不剥；无需改终态枚举/护栏。

### 3.2 Requirements Pool

| 优先级 | 需求 | 验收标准 |
|--------|------|----------|
| **P0** | `locate_violations` 对 `self_consistency` 跨段矛盾**每条矛盾产 1 条 `paragraph` 级 evidence 段**（`paragraph_index`=evidence 待剥），并带 `related_paragraph_index`=claim 段（上下文、不剥）、`pairing`=方向 | 用 5 段 realistic 样本（涨停 claim 段1、负收益 evidence 段3），断言 `self_consistency` 段 `granularity=="paragraph"`、`paragraph_index==3`、`related_paragraph_index==1`、`related != paragraph_index`、`pairing=="limit_up_vs_negative_pct"`；**无任何 `document` 级 `self_consistency` segment** |
| **P0** | degrade **只剥 evidence 段、保留 claim（related）段** → 复检通过 | 上述样本经 `assemble_degraded` → `ok=True`、`removed_ratio < 0.5`、降级稿**不再含**负收益 evidence 但**仍含**涨停 claim 文案、复检 `validate().passed == True` |
| **P0** | 端到端经 `repair_report`（max_rounds=2, safe_degrade_enabled=True）返回 `emit_degraded` | `final_action == "emit_degraded"` 而非 `"reject"`；用户拿到非空白降级稿 |
| **P1** | 单段内自相矛盾（同段含「涨停」且「-0.12%」）行为不变 | 旧单段用例仍定位到该单段 paragraph 级 segment、`related_paragraph_index is None`（无关联段），不触发跨段配对 |
| **P1** | 其他 check（red_line / impossible_move / ungrounded / numeric_source / llm_judge）的 document 级回退逻辑不受影响 | 这些 check 的 `locate_violations` 分支与原行为逐字节一致 |
| **P2** | 多对跨段矛盾（如多只股票各带一份矛盾）全部配对定位 | 探针含 2 份跨段矛盾 → 产出 2 条 evidence 段（各带 `related`），降级剥离 2 evidence 段后复检通过（受 50% 比例护栏约束） |
| **P2** | 新增单测 `tests/test_self_consistency_pairing.py`（已含 `test_p1_5_qa_verification.py`）覆盖 P0/P1 | CI 绿；明确断言「产 evidence 段 + related 上下文、只剥 evidence 不剥 claim」 |

### 3.3 关键不变量 / 约束（架构师已裁决，必须守住）
- `max_rounds` 维持 **2**（不借本次改动调整修复轮数）。
- `max_removed_ratio` 维持 **0.5**（剥超过半篇仍回退 reject，防空壳降级）。
- **告警暂不定**：本次不新增告警通道，仅内部定位 + 降级。
- **document 级不做**：`self_consistency` 不再产出 document 级 segment（矛盾必可配对待剥离）。
- 定位层失败 **绝不反噬** 判定主流程（`validate` 已有 try/except 兜底，保持）。

### 3.4 UI Design Draft
纯后端校验/降级链路，**无 UI**。数据流示意：

```
报告文本
  │
  ▼ validate()  →  self_consistency 全文 fail（跨段矛盾）
  │
  ▼ locate_violations()
  │     ├─ self_consistency 分支：
  │     │     ├─ 单段复跑命中 → 该段 paragraph 级 segment   (沿用，无 related)
  │     │     └─ 未命中 → _locate_self_consistency()
  │     │              → 1 条 evidence 段(paragraph_index=evidence)
  │     │                + related_paragraph_index=claim(上下文,不剥)  (新增·配对)
  │     └─ 其他 check：维持原 document/paragraph 逻辑
  │
  ▼ repair_report() 轮耗尽 → _try_degrade()
  │     └─ assemble_degraded() 仅剥 paragraph_index=evidence 段 + 插占位符 + 复检
  │
  ▼ final_action = "emit_degraded"  →  非空白降级稿送达用户
```

### 3.5 Open Questions（已裁决，留痕）
- ❓ 跨段矛盾是否允许回退 document 级？→ **否**（架构师：document 级不做）。
- ❓ 剥离比例上限是否放宽？→ **否**，保持 50%。
- ❓ 是否顺带加告警？→ **否**，暂不定。
- ❓ 修复轮数是否增加？→ **否**，维持 2 轮。

### 3.6 设计裁决对齐说明（修订留痕）
本 PRD 初版曾把跨段矛盾描述为「矛盾双方各产一条可剥 paragraph 级 segment（段A 声明侧、段B 数值侧），`idxs == {0,1}`」。经 QA 验证报告指出该描述滞后于架构师最终裁决，已据 Bob《系统设计 P1.5》Part A §5.1 / 共享知识 #1 修订为：

- **只产 1 条 paragraph 级 segment**：`paragraph_index` = evidence 段（含矛盾数字 -x%，待剥离），claim 段（含涨停/跌停标记）作为新增字段 `related_paragraph_index` 仅作上下文、**不剥离**；
- **修订理由**：若把 claim 段也定为可剥段落，2 段极简样本下两段 `removed_ratio` 会达 1.0（或 >50% 边界）触发 `max_removed_ratio=0.5` 护栏 → 又回退 reject → 又是空白报告，等于没修 P1.5 根因；
- 验收探针相应改为 ≥5 段 realistic 样本，并补端到端 `repair_report(final_action=="emit_degraded")` 断言，与 `tests/test_p1_5_qa_verification.py` 一致。

---

## 4. 验收探针（交给 QA / 工程师做回归）

```python
# 预期：跨段 self_consistency 矛盾 → 产 1 条 evidence 段(+related 上下文)
#        → degrade 只剥 evidence → emit_degraded（注意：须用 ≥5 段 realistic 样本，
#        否则仅 2 段时剥 1 段即 >50% 命中 max_removed_ratio=0.5 护栏回退 reject）
from src.core.validator import validate
from src.core.degrade import DEGRADED_PLACEHOLDER, assemble_degraded
from src.core.repair import repair_report

# 5 段 realistic 样本：标题 / 涨停 claim(段1) / 技术面 / 负收益 evidence(段3) / 引用
text = (
    "# 600036 招商银行 复盘\n\n"
    "该股今日强势涨停，封板后全天无抛压，资金高度认可。\n\n"   # 段1：claim（涨停）
    "技术面看量能温和放大，换手充分，短期趋势仍强。\n\n"
    "该股今日收跌 -0.12%，量能萎缩，短期承压回落。\n\n"        # 段3：evidence（负收益）
    "> 以上分析基于公开数据，不构成投资建议。"
)

res = validate(text)
assert not res.passed                                  # 全文自洽性 fail
segs = [s for s in res.violation_segments if s.check == "self_consistency"]
assert len(segs) == 1
s = segs[0]
assert s.granularity == "paragraph"                    # 不再 document 级
assert s.paragraph_index == 3, "待剥段 = evidence（含 -0.12%）"
assert s.related_paragraph_index == 1, "关联段 = claim（涨停），仅上下文"
assert s.related_paragraph_index != s.paragraph_index
assert s.pairing == "limit_up_vs_negative_pct"

dres = assemble_degraded(text, segs)
assert dres.ok                                         # 只剥 evidence 段、复检通过
assert dres.removed_ratio < 0.5                        # 5 段样本剥 1 段 <50%
assert "收跌 -0.12%" not in dres.degraded_text          # evidence 已剥
assert "强势涨停" in dres.degraded_text                # claim 上下文保留
assert validate(dres.degraded_text).passed is True

# 端到端贯通：repair loop 失败（LLM 不可用）→ 走 degrade → emit_degraded（非 reject）
def _llm_unavailable(*a, **k):
    raise RuntimeError("simulate LLM unavailable")
rres = repair_report(
    text,
    reasons=[f"[自相矛盾] {s.reason}"],
    violation_segments=segs,
    max_rounds=2,
    llm_call=_llm_unavailable,
    safe_degrade_enabled=True,
)
assert rres.final_action == "emit_degraded"   # 跨段矛盾 → 降级交付，而非空白 reject
```

> 说明：QA 已按上述权威设计编写 `tests/test_self_consistency_pairing.py` 与 `tests/test_p1_5_qa_verification.py`，断言「产 evidence 段 + related 上下文、只剥 evidence 不剥 claim」，未因本 PRD 旧表述误报。

---

## 5. 交付清单（本 PRD 给到下游）
- ✅ 本增量 PRD（产品目标 + 改动点 + 验收探针 + 不变量）
- ➡️ 下游：**架构师** Bob 已出《系统设计 P1.5》→ **工程师**已实现 → **QA** 已按权威设计跑通 `tests/test_self_consistency_pairing.py` / `test_p1_5_qa_verification.py`（本 PRD §3.1/§3.2/§4 已与之一致对齐）
